/**
 * Search Types
 *
 * The only active search strategy is ENRICHED_V2 (enrichedSearchV2).
 * These types are kept for API compatibility.
 */

/**
 * Supported search strategies.
 */
export type SearchType = 'ENRICHED_V2';
