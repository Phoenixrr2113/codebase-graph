{
  "name": "@codegraph/plugin-python",
  "version": "0.1.0",
  "description": "Python language plugin for CodeGraph",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root . --config ../../vitest.package.config.ts"
  },
  "dependencies": {
    "@codegraph/logger": "workspace:*",
    "@codegraph/plugin-common": "workspace:*",
    "@codegraph/plugin-generic": "workspace:*",
    "@codegraph/types": "workspace:*",
    "tree-sitter": "^0.25.1",
    "tree-sitter-python": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
